export const PageTitle = ({children}: {children: React.ReactNode}) => (
	<h1 className="dark:to-primary-40 mb-8 bg-gradient-to-b from-copy-base from-50% to-primary-600 bg-clip-text px-2 text-4xl font-semibold text-transparent">
		{children}
	</h1>
)
