import styles from './styles.module.css'

export const Mission = () => (
	<section
		className={`${styles.container} mb-8 flex flex-col items-center px-2 py-16 text-center text-3xl lg:mb-16 lg:py-32 lg:text-4xl`}
	>
		<span>I code around the world,</span>
		<span>and inspire others to do the same!</span>
	</section>
)
